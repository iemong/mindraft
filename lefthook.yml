#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - run: bun run format
      glob: "*.{js,ts,jsx,tsx}"
      stage_fiexed: true
